## 21. 最重要的安全原则

1. 不要随便提交 `.env`、密钥、密码、token。
2. 不要直接在 `main` 分支乱改。
3. 不要看到报错就乱删文件夹。
4. 不要手动改 `node_modules`。
5. 不要手动乱改锁文件，除非你知道为什么。
6. 不要把 README 里的命令跨项目乱用。
7. 不要把全局配置当成项目配置乱改，比如全局 `.npmrc`。
8. 让 Codex 改代码前，先确认目标和范围。
9. Codex 改完后，一定看改了哪些文件。
10. 不懂时先让 Codex 解释，不要马上让它大改。

---
